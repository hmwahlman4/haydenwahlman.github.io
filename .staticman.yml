comments:
  allowedFields: ["name", "message"]
  requiredFields: ["name", "message"]
  branch: "main"
  path: "_data/comments"
  format: "json"
  moderation: false

  generatedFields:
    timestamp:
      type: date
      options:
        format: timestamp

  filename: "comment-{@timestamp}.json"
